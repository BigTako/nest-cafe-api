export enum CurUserOrderStatus {
  Cancelled = 'cancelled',
  Resolved = 'resolved',
  New = 'new',
}

export enum Status {
  New = 'new',
  Paid = 'paid',
  Unpaid = 'unpaid',
  Resolved = 'resolved',
  Cancelled = 'cancelled',
}
